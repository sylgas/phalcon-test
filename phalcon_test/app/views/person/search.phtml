<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("person/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("person/new", "Create ")); ?>
        </td>
    </tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
            <th>Firstname</th>
            <th>Lastname</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $person) { ?>
        <tr>
            <td><?php echo $person->id ?></td>
            <td><?php echo $person->firstname ?></td>
            <td><?php echo $person->lastname ?></td>
            <td><?php echo $this->tag->linkTo(array("person/edit/" . $person->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("person/delete/" . $person->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("person/search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("person/search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("person/search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("person/search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    <tbody>
</table>
